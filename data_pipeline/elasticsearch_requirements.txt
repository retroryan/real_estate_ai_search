# Elasticsearch Spark Connector Requirements
# These JARs need to be available to Spark at runtime

# Option 1: Use Maven coordinates when starting Spark
# spark-submit --packages org.elasticsearch:elasticsearch-spark-30_2.12:8.11.4

# Option 2: Download JAR and add to classpath
# Download from: https://mvnrepository.com/artifact/org.elasticsearch/elasticsearch-spark-30_2.12
# Version: 8.11.4 (compatible with Spark 3.x and Elasticsearch 8.x)

# Option 3: Add to SparkSession config in code
# .config("spark.jars.packages", "org.elasticsearch:elasticsearch-spark-30_2.12:8.11.4")

# Python requirements (already in main pyproject.toml)
# pyspark>=3.5.0