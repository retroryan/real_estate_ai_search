# Core dependencies for Wikipedia crawler
requests>=2.31.0
pydantic>=2.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# For database operations
# sqlite3 is built-in to Python

# Optional but useful
python-dotenv>=1.0.0  # For environment variables
tqdm>=4.65.0  # For progress bars